#ifndef RISKDEFINES_H
#define RISKDEFINES_H




typedef enum 
{
	OK = 0,
	ARGUMENT_WRONG,
	PLAYER_NOT_ALLOWED,
	TARGET_WRONG,
	NOT_ENOUGH_ARMYS,
	TOO_MUCH_ARMYS,
	NO_PATH_FOUND,
	CARD_NOT_AVAILABLE,
	NO_SET,
	CARDS_ALREADY_EXCHANGED,
	NOT_ENOUGH_FREE_ARMYS,
	TOO_MUCH_CARDS



} RISK_ERROR;

typedef enum
{
	INIT = -1,
	ADD_ARMYS = 0,
	ATTACK,
	MOVE
} GAMEPHASE;

typedef enum
{
	INFANTRY = 0,
	CAVALRY,
	ARTILLERY
} CARDSYMBOL;

typedef enum
{
	FOREVER_FIVE = 0,
	ASCENDING_BY_TIME,
	BY_SYMBOL
} CARDEXCHANGEOPTION;

typedef enum
{
	ONLY_NEIGHBOR = 0,
	ONLY_PATH
} MOVEOPTION;


#endif